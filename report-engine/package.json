{
  "name": "zhixing-report-engine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Offline, fixture-only market research report engine prototype.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "preview:visual": "npm run build && node scripts/generate-visual-previews.mjs"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
